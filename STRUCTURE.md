# Структура проекта

## 🗂️ Где что находится

### 📄 Страницы (app/)
- `app/page.tsx` - Главная страница (показывает Финский зал)
- `app/rooms/[slug]/page.tsx` - Страницы комнат (fin, fin-small, turkey, apps)
- `app/layout.tsx` - Корневой layout с метаданными

### 🧩 Виджеты (widgets/)
Крупные составные блоки интерфейса:

- **`widgets/header/`** - Хедер сайта
  - `ui/header.tsx` - Основной компонент хедера
  - `ui/mobile-menu.tsx` - Мобильное меню
  - `model/useHeader.ts` - Логика управления меню

- **`widgets/room-display/`** - Виджет страницы комнаты
  - `ui/room-display.tsx` - Объединяет Header + Slideshow + RoomView

### ⚙️ Фичи (features/)
Бизнес-логика, которая несет ценность для пользователя:

- **`features/room-slideshow/`** - Слайдшоу комнат
  - `ui/room-slideshow.tsx` - Компонент слайдшоу
  - `model/useSlideshow.ts` - Логика слайдшоу (состояние, таймеры)

- **`features/view-room/`** - Отображение информации о комнате
  - `ui/room-view.tsx` - Компонент с описанием, фичами, кнопкой бронирования

### 🏗️ Сущности (entities/)
Бизнес-сущности проекта:

- **`entities/room/`** - Комнаты (залы)
  - `api/roomData.ts` - Данные всех комнат
  - `model/types.ts` - Типы RoomInfo, RoomSlug
  - `model/useRoom.ts` - Хук для работы с комнатами

- **`entities/feature/`** - Особенности залов
  - `api/featureData.ts` - Данные всех фич (featuresById)
  - `model/types.ts` - Типы FeatureId, FeatureSpec

### 🔧 Shared (shared/)
Переиспользуемый код без бизнес-логики:

- **`shared/ui/`** - UI-кит компонентов
  - `button.tsx`, `badge.tsx`, `card.tsx` - Базовые компоненты
  - `feature-pill.tsx` - Компонент буллета особенности
  - `logo.tsx` - Логотип
  - `icons/` - Иконки (PhoneIcon, TelegramIcon, WhatsappIcon)

- **`shared/lib/`** - Утилиты
  - `utils.ts` - Вспомогательные функции (cn, formatDate, etc.)

- **`shared/config/`** - Конфигурация
  - `ui.ts` - Настройки слайдшоу (SLIDESHOW_CONFIG, SLIDESHOW_INTERVAL_MS)

## 📝 Где редактировать контент

### Комнаты (залы)
**Файл**: `src/entities/room/api/roomData.ts`
- Названия, описания, адреса, вместимость
- Пути к изображениям
- Какие фичи есть у каждой комнаты

### Особенности залов (фичи)
**Файл**: `src/entities/feature/api/featureData.ts`
- Тексты фич (label)
- Пути к иконкам (iconPath)

### Настройки слайдшоу
**Файл**: `src/shared/config/ui.ts`
- Длительность показа каждого слайда
- Настройки анимации

### Хедер и навигация
**Файл**: `src/widgets/header/ui/header.tsx`
- Ссылки на соцсети
- Навигационное меню
- Кнопки бронирования

## 🔄 Поток данных

1. **Страница** (`app/rooms/[slug]/page.tsx`)
   - Получает данные комнаты из `entities/room`
   - Передает в `widgets/room-display`

2. **Виджет RoomDisplay**
   - Использует `features/room-slideshow` для фона
   - Использует `widgets/header` для хедера
   - Использует `features/view-room` для контента

3. **Feature ViewRoom**
   - Получает данные фич из `entities/feature`
   - Использует `shared/ui/feature-pill` для отображения

## 📦 Импорты

Все импорты абсолютные через алиасы:
- `@/widgets/*` - виджеты
- `@/features/*` - фичи
- `@/entities/*` - сущности
- `@/shared/*` - shared код

